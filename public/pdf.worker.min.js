// This is a placeholder for the PDF.js worker
// In a real deployment, you would copy the actual pdf.worker.min.js from node_modules/pdfjs-dist/build/
// For now, we'll use the CDN version as fallback

// Redirect to CDN version
if (typeof window !== 'undefined') {
  importScripts('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js');
}